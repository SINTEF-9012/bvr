x;

if scanner->size() > 0 then 
	printer->collect(resolution)->max() >= scanner->collect(resolution)->max()
else
	true
endif;

(scanner->size() = 0) or (printer->collect(resolution)->max() >= scanner->collect(resolution)->max())

resolution = 300 or resolution = 600 or resolution = 1000;

resolution = 300 or resolution = 1000;